spring.application.name=patient-service


#we added H2 database for testing purpose in memory Database , then we will switch to postgres for production
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=admin_viewer
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
#this will reform the checks against DB and Entities
spring.sql.init.mode=always
#server.port=4000

#change this to postgres it does not work for unknown reason